# Deep Learning - CNN ResNet
torch>=2.0.0
torchvision>=0.15.0

# Visión por computadora
opencv-python>=4.5.0

# Computación científica
numpy>=1.24.0

# Cliente HTTP para API de iNaturalist (curador + descargador)
requests>=2.25.0

# ── Descargador de omnívoros extintos (imagenes.py) ──────────────────────────
# Lee omnivore_extinct_candidates.csv y baja imágenes de Google Imágenes (Selenium).
selenium>=4.6.0          # automatiza Chrome (chromedriver lo resuelve Selenium solo)
Pillow>=9.0.0             # filtro de resolución/conversión de imagen
# pytesseract>=0.3.10     # OPCIONAL: descarta imágenes con texto incrustado (requiere Tesseract)

# ── Descargador de datasets (download.py) ────────────────────────────────────
# La fase B (suplemento desde iNaturalist) solo necesita 'requests', ya incluido.
# La fase A (datasets masivos) usa estos paquetes según la fuente; si falta uno,
# esa fuente se omite con aviso y el resto continúa. Descoméntalos si los usas:
kaggle>=1.6.0              # datasets de Kaggle (requiere ~/.kaggle/kaggle.json)
# huggingface_hub>=0.20.0 # datasets de Hugging Face
# roboflow>=1.1.0         # datasets de Roboflow (requiere ROBOFLOW_API_KEY)
